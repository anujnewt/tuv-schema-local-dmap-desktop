-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
-- dmap_object_gen_tag : type : function name : immutable_to_char(timestamp,
set search_path = fecxc,oracle,dmap_extension,public;
create or replace function immutable_to_char(timestamp, fmt text) returns text as
$$ select to_char($1, $2); $$
language sql immutable;
