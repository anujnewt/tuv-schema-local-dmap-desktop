CREATE OR REPLACE EDITIONABLE FUNCTION "FECXC"."FECXC_ID_TITULO_FN" (
								  PST_NOM_TITULO IN VARCHAR2)
RETURN NUMBER
  AS
-- PGV moved types start

-- PGV moved types end

-- PGV moved types start
-- PGV moved types end
    PII_ID_TITULO NUMBER;
  BEGIN
    SELECT ID_TITULO
    INTO PII_ID_TITULO
    FROM FECXC.XXFECXC_TIT_REP_COMER_TAB
    WHERE DES_TITULO = PST_NOM_TITULO;
    RETURN PII_ID_TITULO;
  END;
/
--Source_DDLS

  CREATE OR REPLACE EDITIONABLE FUNCTION "FECXC"."FECXC_ID_TITULO_FN" (
								  PST_NOM_TITULO IN VARCHAR2)
RETURN NUMBER
  AS
-- PGV moved types start

-- PGV moved types end

-- PGV moved types start
-- PGV moved types end
    PII_ID_TITULO NUMBER;
  BEGIN
    SELECT ID_TITULO
    INTO PII_ID_TITULO
    FROM FECXC.XXFECXC_TIT_REP_COMER_TAB
    WHERE DES_TITULO = PST_NOM_TITULO;
    RETURN PII_ID_TITULO;
  END;
/
