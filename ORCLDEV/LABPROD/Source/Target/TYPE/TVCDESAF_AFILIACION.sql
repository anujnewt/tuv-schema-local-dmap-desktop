-- DMAP_OBJECT_GEN_TAG : TYPE : EDITIONABLE NAME : tvcdesaf_afiliacion
SET search_path = labprod,oracle,dmap_extension,public;

CREATE TYPE tvcdesaf_afiliacion AS (
keycon varchar, porcen NUMERIC, cuofij NUMERIC

);
