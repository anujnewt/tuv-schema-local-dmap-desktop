CREATE OR REPLACE NONEDITIONABLE FUNCTION "LABPROD"."SP_GLGETFEC" 
RETURN DATE IS
-- PGV moved types start

-- PGV moved types end

-- PGV moved types start
-- PGV moved types end
BEGIN
    RETURN TO_DATE (SYSDATE, 'mm/dd/yyyy');
END;
/
--Source_DDLS

  CREATE OR REPLACE NONEDITIONABLE FUNCTION "LABPROD"."SP_GLGETFEC" 
RETURN DATE IS
-- PGV moved types start

-- PGV moved types end

-- PGV moved types start
-- PGV moved types end
BEGIN
    RETURN TO_DATE (SYSDATE, 'mm/dd/yyyy');
END;
/
