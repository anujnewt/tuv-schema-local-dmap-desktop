CREATE TABLE "LABCONF"."WEBITACORA" 
   (	"BIT_KEYEMP" NUMBER(*,0) NOT NULL ENABLE, 
	"BIT_MODULO" VARCHAR2(50) NOT NULL ENABLE, 
	"BIT_FECHA" DATE, 
	"BIT_HORA" VARCHAR2(8), 
	"BIT_IP" VARCHAR2(20)
   ) ;
